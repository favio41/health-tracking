{
	"permissions": {
		"allow": ["Bash(npm run *)", "Bash(grep *)", "Bash(find *)"],
		"deny": ["Read(./notes)", "Read(./data)"]
	},
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "command",
						"command": "npm run format",
						"statusMessage": "Formatting code..."
					}
				]
			},
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "command",
						"command": "npx tsc --noEmit",
						"statusMessage": "Validating TypeScript..."
					}
				]
			}
		]
	}
}
